# SDKWork Assets — Local Workspace Metadata

Source-controlled SDKWork workspace metadata for `sdkwork-assets`.

Local-only state belongs under `local/`, `tmp/`, `cache/`, `secrets/`, and `manual-backups/` (gitignored).
