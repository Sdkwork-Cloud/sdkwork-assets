# SDKWork Workspace Metadata

This directory contains source-controlled workspace metadata for the SDKWork Assets common package root.

- `skills/` - Common-root local skills when needed.
- `plugins/` - Common-root local plugins when needed.
