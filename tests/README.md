# Tests

- `repository-structure.test.mjs` — L1 baseline and workspace layout
- `architecture-alignment.test.mjs` — framework posture and Drive SDK integration

Run: `pnpm topology:test` or `node --test tests/*.test.mjs`
